/** Mid-range detailScale for static path shape computation */
export const S = 0.76;

/** Bounding box target: each curve fills ±TARGET of the 100-unit viewBox */
export const TARGET = 36;

/** Default number of sample points for path generation */
export const DEFAULT_STEPS = 480;
